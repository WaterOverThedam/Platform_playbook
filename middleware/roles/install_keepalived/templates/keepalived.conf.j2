! Configuration File for keepalived

global_defs {
    notification_email {
        zhojy@outlook.com
    }
    notification_email_from M1@keepalived.com
    smtp_server 127.0.0.1
    smtp_connect_timeout 30
    router_id {% if inventory_hostname == first %} M1 {% else %} {{inventory_hostname|regex_replace('mid','B')}} {% endif %}

}
